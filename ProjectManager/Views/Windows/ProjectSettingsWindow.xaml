<Window x:Class="ProjectManager.UI.Views.ProjectSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectManager.UI.Views"
        xmlns:p="clr-namespace:ProjectManager.UI.Properties"
        mc:Ignorable="d"
        Height="100" Width="400">
    <StackPanel>
        <TextBlock Width="350" Text="{x:Static p:Resources.Title}"/>
        <TextBox Width="350" Text="{Binding Name}"/>
        <Button Margin="5" Content="{x:Static p:Resources.Save}" Width="100" IsDefault="True" Click="Button_Click"/>
    </StackPanel>
</Window>
